customtkinter>=5.2.2
opencv-python-headless>=4.9.0.80
numpy>=1.26.0
requests>=2.31.0
cryptography>=42.0.0
websockets>=12.0
Pillow>=10.2.0
# DirectML build: ships the CPU provider plus DmlExecutionProvider, so the
# model screen can detect GPU acceleration on any DirectX 12 GPU (NVIDIA/AMD/
# Intel) on the Windows target. Also used later for ONNX inference.
onnxruntime-directml>=1.17.0
